# .npmrc
# ------
# Konfigurationsdatei für npm, die das Verhalten des Paketmanagers steuert

# Sorgt dafür, dass npm detaillierte Fehlermeldungen anzeigt
loglevel=error

# Speichert eine exakte Version der abhängigen Pakete
save-exact=true

# Voreinstellungen für neue Pakete
init-version=0.0.1
init-license=MIT
init-author-name=
init-author-email=

# Maximale Anzahl von Netzwerkwiederholungen bei Verbindungsproblemen
fetch-retries=3

# Timeout für Anfragen in Millisekunden
fetch-timeout=60000

# Verhindert, dass npm Skripte automatisch ausführt (für mehr Sicherheit)
# Entfernen Sie diesen Kommentar, wenn Sie Skripte manuell ausführen möchten
# ignore-scripts=true