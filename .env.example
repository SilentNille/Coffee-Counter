# .env.example
# -----------
# Diese Datei dient als Vorlage für die .env-Datei
# Sie enthält Beispielwerte für Umgebungsvariablen, die in der App verwendet werden
# Für die tatsächliche Entwicklung sollte eine Kopie dieser Datei als .env gespeichert werden
# Die .env-Datei sollte nicht im Git-Repository gespeichert werden (daher in .gitignore aufgeführt)

# Server-Konfiguration
PORT=3000
NODE_ENV=development

# Datenbank-Konfiguration (für zukünftige Verwendung)
DB_HOST=localhost
DB_PORT=27017
DB_NAME=testapp_db
DB_USER=user
DB_PASSWORD=password

# JWT-Secret für Authentifizierung (in einer echten App ein sicheres Secret verwenden)
JWT_SECRET=beispiel_secret_key

# API-Limits
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100